{
  "name": "nest-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --workspaces",
    "start": "node --enable-source-maps --loader ./misc/loader.mjs packages/admin_backend/src/main.ts",
    "lint": "npm run lint --workspaces"
  },
  "dependencies": {
    "typescript": "^5.0.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
